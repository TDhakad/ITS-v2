-------------------------------------------------- 
manifest update time: 2026-04-28 21:27:04.372262
ingested time: 2026-04-28 21:27:04.372262
-------------------------------------------------- 
CREATE TABLE ticket_tags (
	id INTEGER NOT NULL, 
	ticket_id INTEGER NOT NULL, 
	tag_id INTEGER NOT NULL, 
	PRIMARY KEY (id), 
	CONSTRAINT uq_ticket_tag UNIQUE (ticket_id, tag_id), 
	FOREIGN KEY(ticket_id) REFERENCES tickets (id) ON DELETE CASCADE, 
	FOREIGN KEY(tag_id) REFERENCES tags (id) ON DELETE CASCADE
)

Column Details:
-- id: Primary key for ticket-tag link row.
-- ticket_id: Foreign key to tickets.id.
-- tag_id: Foreign key to tags.id.

HINTS: 
To join 'tags' and 'ticket_tags', use tags.id = ticket_tags.tag_id
To join 'ticket_tags' and 'tickets', use ticket_tags.ticket_id = tickets.id